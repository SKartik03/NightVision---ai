version: '3.8'

services:
  nightvision-ai:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: nightvision-ai
    ports:
      - "8000:8000"
    environment:
      - PORT=8000
      - MODEL_MODE=DEMO
    restart: unless-stopped
